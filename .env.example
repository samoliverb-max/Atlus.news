# Postgres connection string. In dev, point this at a local Postgres instance
# (or run the e2e tests, which spin up their own ephemeral one and need no env var).
DATABASE_URL=postgres://localhost:5432/atlus

# Port the onboarding API listens on.
PORT=4000
